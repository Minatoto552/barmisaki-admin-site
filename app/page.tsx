import { redirect } from 'next/navigation';
import { chatGPTSignInPath, getChatGPTUser } from './chatgpt-auth';
export const dynamic = 'force-dynamic';
export default async function LoginPage() { const user = await getChatGPTUser(); if (user) redirect('/dashboard'); return <main className="grid min-h-dvh place-items-center bg-[radial-gradient(circle_at_50%_0%,#ede9fe,transparent_45%),#f8fafc] p-5"><section className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 shadow-[0_24px_80px_rgba(15,23,42,.08)] sm:p-10"><div className="mb-8 grid size-12 place-items-center rounded-2xl bg-violet-600 text-2xl text-white">M</div><p className="text-xs font-bold tracking-[.16em] text-violet-600">BARMISAKI ADMIN</p><h1 className="mt-3 text-3xl font-bold tracking-tight">運営者ログイン</h1><p className="mt-4 text-sm leading-7 text-slate-500">このサイトはBarMisaki運営者専用です。管理画面を利用するにはChatGPTアカウントでログインしてください。</p><a href={chatGPTSignInPath('/dashboard')} target="_top" className="mt-8 flex h-12 items-center justify-center rounded-xl bg-slate-950 text-sm font-bold text-white transition hover:bg-violet-700">ChatGPTでログイン</a><p className="mt-5 text-center text-xs text-slate-400">許可された管理者のみアクセスできます</p></section></main>; }
